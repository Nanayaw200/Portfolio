import siteContent from "./siteContent";

const testimonials = [
  {
    id: 1,
    quote: siteContent.testimonial.quote,
    name: siteContent.testimonial.name,
    role: siteContent.testimonial.role,
  },
];

export default testimonials;